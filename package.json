{
  "name": "formmail-functions",
  "description": "Backend de FormMail (clon de Formspree) — Firebase Functions",
  "engines": {
    "node": "20"
  },
  "main": "src/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0",
    "cloudinary": "^2.2.0",
    "busboy": "^1.6.0",
    "resend": "^3.2.0"
  },
  "private": true
}
